/**
 * features.ts — Interruptores de secciones completas del sitio.
 * Cambiar a `true` reactiva la sección sin tocar el resto del código.
 */

// El cliente no necesita la sección de Obras por ahora. El contenido y el
// código siguen intactos; solo se oculta del sitio público hasta que se
// reactive.
export const OBRAS_HABILITADAS = false;
