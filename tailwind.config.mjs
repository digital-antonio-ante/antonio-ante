/**
 * Tailwind CSS v4 — este archivo NO configura nada.
 *
 * En v4 el tema se declara en CSS con `@theme`, y en este proyecto vive ENTERO
 * en src/styles/global.css. Se mantiene el archivo solo porque algunas
 * herramientas (IntelliSense de editores, extensiones) buscan un
 * `tailwind.config.*` para activarse.
 *
 * NO documentes la paleta aquí. Hasta agosto de 2026 este archivo describía una
 * identidad institucional (#003366 azul, #C5A059 dorado) que no se usaba en
 * ninguna parte del sitio: la implementada es la paleta "Bosque Vivo" (verdes)
 * de global.css. Dos fuentes de verdad para el color significan que una miente
 * — y ésta mintió durante meses.
 *
 * Paleta real, tokens y ratios de contraste: src/styles/global.css
 * Assets de marca del manual del cantón:     public/marca/
 */
export default {};
